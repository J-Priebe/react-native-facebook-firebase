'use strict'

import React, {
  StyleSheet,
} from 'react-native';

module.exports = StyleSheet.create({

  container: {
    alignItems:'center',
    justifyContent:'center',
    flex: 1
  },

  body: {
    flex: 1,
    alignItems:'center',
    justifyContent:'center',
  }
});
